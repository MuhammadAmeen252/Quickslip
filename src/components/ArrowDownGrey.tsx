import React from 'react';
import {Image} from 'react-native';

const ArrowDownGrey = () => {
  return (
    <Image
      style={{height: 10, width: 16}}
      source={require('../assets/icons/arrow_down_grey.png')}
    />
  );
};

export default ArrowDownGrey;
